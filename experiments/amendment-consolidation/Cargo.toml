[package]
name = "amendment-consolidation"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
legal-structure = { path = "../..", features = ["structure-inference", "document-query"] }
legal-grammar-tables = { path = "../../grammar" }
regex = "1.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
