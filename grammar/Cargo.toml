[package]
name = "legal-grammar-tables"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
fancy-regex = "0.19"
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
