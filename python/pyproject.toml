[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "legal-structure"
version = "0.1.0"
description = "Native legal-document structure compilation"
requires-python = ">=3.11"

[tool.maturin]
module-name = "legal_structure"
