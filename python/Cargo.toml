[package]
name = "legal-structure-python"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "legal_structure"
crate-type = ["cdylib"]

[dependencies]
legal-structure-core = { package = "legal-structure", path = "..", features = ["a2aj", "journal"] }
pyo3 = { version = "0.29.2", features = ["extension-module"] }
